# Reports whether x is a RoBMA object

Reports whether x is a RoBMA object

## Usage

``` r
is.RoBMA(x)

is.RoBMA.reg(x)

is.NoBMA(x)

is.NoBMA.reg(x)

is.BiBMA(x)
```

## Arguments

- x:

  an object to test

## Value

returns a boolean.
