% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brma.mv.R
\name{brma.mv}
\alias{brma.mv}
\title{Bayesian Meta-Analysis With Known Sampling Covariance}
\usage{
brma.mv(
  yi,
  V,
  ni,
  mods,
  scale,
  random,
  R = NULL,
  Rscale = "cor",
  data,
  slab,
  subset,
  measure,
  prior_effect,
  prior_heterogeneity,
  prior_mods,
  prior_scale,
  standardize_continuous_predictors = TRUE,
  set_contrast_factor_predictors = "treatment",
  prior_unit_information_sd,
  rescale_priors = 1,
  prior_informed_field,
  prior_informed_subfield,
  known_v_parameterization = "auto",
  known_v_residual_fraction = 0.1,
  marginalize_estimate_level = TRUE,
  sample = 5000,
  burnin = 2000,
  adapt = 500,
  chains = 3,
  thin = 1,
  parallel = FALSE,
  autofit = FALSE,
  autofit_control = set_autofit_control(),
  convergence_checks = set_convergence_checks(),
  seed = NULL,
  silent,
  ...
)
}
\arguments{
\item{yi}{a vector of effect sizes, or a formula with the effect size on the
left-hand side and location moderators on the right-hand side (for example
\code{yi ~ x1 + x2}). If a formula is supplied, \code{mods} must not be specified.}

\item{V}{a known working variance-covariance matrix, or a list of block
variance-covariance matrices, used by \code{brma.mv()}.}

\item{ni}{an optional vector of sample sizes. Used for \code{measure = "GEN"} or
when estimating the unit information standard deviation.}

\item{mods}{an optional matrix, data.frame, or formula specifying
location moderators (meta-regressors). Formula input is evaluated in \code{data}.}

\item{scale}{an optional matrix, data.frame, or formula specifying
scale predictors for location-scale models. Formula input is evaluated in
\code{data}.}

\item{random}{an optional formula or list of formulas specifying
BayesTools random-effect terms for \code{brma.mv()}. Use
\link[=random_effect_formula_tags]{random-effect formula structure tags} such as
\code{diag()}, \code{us()}/\code{un()}, \code{cs()}, \code{hcs()}, \code{ar1()}/\code{ar()}, \code{har()}, or
\code{car()} inside the formula. Plain random-effect syntax is allowed only for
random intercepts; random slopes require an explicit structure tag or the
\code{||} diagonal shorthand.}

\item{R}{optional known random-effect group covariance/correlation matrix,
or a named list of matrices. Matrix row and column names identify grouping
levels. This is distinct from the known sampling covariance \code{V}.}

\item{Rscale}{scaling applied to each supplied \code{R} matrix before fitting.
Supported values are \code{"cor"}, \code{"none"}, \code{"cor0"}, \code{"cov0"}, with
metafor-compatible aliases \code{TRUE}, \code{FALSE}, \code{0}, \code{1}, \code{2}, and \code{3}.}

\item{data}{an optional data frame containing the variables.}

\item{slab}{an optional vector of study labels.}

\item{subset}{an optional logical or numeric vector specifying a subset of
data to be used.}

\item{measure}{a character string specifying the effect size measure.
Normal/effect-size constructors require an explicit value and support
\code{"SMD"}, \code{"ZCOR"}, \code{"RR"}, \code{"OR"}, \code{"HR"}, \code{"RD"}, \code{"IRR"}, and \code{"GEN"}.
Use \code{"GEN"} only for general effect sizes without a known unit information
standard deviation. GLMM raw-count constructors support only \code{"OR"} and
\code{"IRR"} and default to \code{"OR"}.}

\item{prior_effect}{prior distribution for the effect size (\eqn{\mu}) parameter
(the intercept). If omitted, a default prior is constructed. In single-model
functions, explicit \code{NULL} or \code{FALSE} sets a spike at zero.}

\item{prior_heterogeneity}{prior distribution for the heterogeneity (\eqn{\tau})
parameter. If omitted, a default prior is constructed. In single-model
functions, explicit \code{NULL} or \code{FALSE} sets a spike at zero.}

\item{prior_mods}{prior distribution for the moderators (\eqn{\beta}) parameters.
A single prior applies to all terms; a named list can specify term-specific
priors. If omitted or \code{NULL}, default priors are used.}

\item{prior_scale}{prior distribution for the scale (\eqn{\delta}) parameters.
A single prior applies to all terms; a named list can specify term-specific
priors. If omitted or \code{NULL}, default priors are used.}

\item{standardize_continuous_predictors}{logical. Whether to standardize continuous predictors.
Defaults to \code{TRUE}.}

\item{set_contrast_factor_predictors}{character. How to set contrast for factor predictors.
Defaults are constructor-specific and shown in each function usage. Single-model
constructors use \code{"treatment"}; model-averaging constructors use \code{"meandif"}.}

\item{prior_unit_information_sd}{numeric. The unit information standard deviation (\eqn{\sigma_{unit}}).
Cannot be used together with \code{prior_informed_field}.}

\item{rescale_priors}{numeric. A scaling factor for supported prior distributions.
Point and none priors are unchanged. For constructors with publication-bias
prior distributions, \code{rescale_priors} does not rescale them except for the
default PEESE prior's UISD adjustment. Defaults to 1.}

\item{prior_informed_field}{character. The field of the informed prior distributions.
Omit to use the standard default prior specification; explicit \code{NULL} is invalid.}

\item{prior_informed_subfield}{character. The subfield of the informed prior distributions.
Can only be specified together with \code{prior_informed_field}. Omit to use the
field-specific default, such as \code{"Cochrane"} for \code{prior_informed_field = "medicine"}; explicit \code{NULL} is invalid.}

\item{known_v_parameterization}{known-\code{V} backend used by \code{brma.mv()}.
\code{"auto"} chooses an exact backend when feasible; \code{"latent"} uses a latent
\verb{D + BB'} decomposition; \code{"whitened"} uses an eigen-rotated normal
likelihood; \code{"block_mvn"} uses an exact native block multivariate-normal
likelihood.}

\item{known_v_residual_fraction}{proportion of the diagonal of \code{V} left as
conditional independent residual sampling variance in the latent \verb{D + BB'}
representation. Defaults to \code{0.10}; values are validated for all backends
but affect only the latent backend. When \code{"auto"} selects \code{"whitened"} or
\code{"block_mvn"}, explicitly supplied values are disregarded by the likelihood.
Direct \code{"whitened"} or \code{"block_mvn"} requests warn when an explicit value is
supplied.}

\item{marginalize_estimate_level}{logical; should a one-to-one estimate-level
random-intercept block be integrated into the likelihood variance instead of
sampled as latent random coefficients? Defaults to \code{TRUE}.}

\item{sample}{numeric. Number of MCMC samples to save. Defaults to \code{5000}.}

\item{burnin}{numeric. Number of burn-in iterations. Defaults to \code{2000}.}

\item{adapt}{numeric. Number of adaptation iterations. Defaults to \code{500}.}

\item{chains}{numeric. Number of MCMC chains. Defaults to \code{3}.}

\item{thin}{numeric. Thinning interval. Defaults to \code{1}.}

\item{parallel}{logical. Whether to run MCMC chains in parallel. Defaults to \code{FALSE}.}

\item{autofit}{logical. Whether to automatically extend the MCMC chains if convergence is not met.
Defaults to \code{FALSE}.}

\item{autofit_control}{list of autofit control settings. See \code{\link[=set_autofit_control]{set_autofit_control()}} for details.}

\item{convergence_checks}{list of convergence check settings. See \code{\link[=set_convergence_checks]{set_convergence_checks()}} for details.}

\item{seed}{numeric. Random seed for reproducibility. Defaults to \code{NULL}.}

\item{silent}{logical. Whether to suppress output. When omitted,
constructors use \code{RoBMA.get_option("silent")}.}

\item{...}{additional advanced arguments. Fitting functions reject unused
arguments; currently recognized internal arguments include \code{only_data},
\code{only_priors}, \code{is_JASP}, and \code{is_JASP_prefix}.}
}
\value{
A fitted object of class \code{c("brma.mv", "brma.norm", "brma")}.
The advanced internal \code{only_data = TRUE} path returns checked data before
prior validation and random-effect marginalization decisions; use
\code{only_priors = TRUE} when inspecting the compiled random-effect structure.
}
\description{
Fits normal-likelihood Bayesian meta-analytic models with a
known working variance-covariance matrix \code{V}. The interface supports
location moderators, BayesTools random-effect formulas using
\link[=random_effect_formula_tags]{random-effect formula structure tags}, and
\code{brma.mv()}-specific known-\code{V} backends.
}
\details{
\code{brma.mv()} represents known sampling dependence through a latent \verb{D + BB'}
decomposition of \code{V} (\code{known_v_parameterization = "latent"}), through an
eigen-rotated likelihood (\code{known_v_parameterization = "whitened"}), or
through an exact native block multivariate-normal likelihood
(\code{known_v_parameterization = "block_mvn"}). The default
\code{known_v_parameterization = "auto"} chooses the whitened backend whenever the
extra diagonal variance is scalar, otherwise chooses block-MVN when the
largest dense covariance block is at or below the configured feasibility
threshold, and falls back to the latent backend for large blocks. The automatic
block-MVN threshold defaults to 128 rows per covariance block and can be
changed with \code{options(RoBMA.known_v_block_mvn_max_block_size = value)}, where
\code{value} is a positive integer or \code{Inf}.
As a convenience for diagonal known-\code{V} inputs, \code{brma.mv()} also accepts
hidden \code{vi} or \code{sei} arguments through \code{...} when \code{V} is omitted; these are
converted to \code{diag(vi)} or \code{diag(sei^2)}. Do not supply \code{V} together with
\code{vi} or \code{sei}.

The \code{random} argument uses BayesTools
\link[=random_effect_formula_tags]{random-effect formula structure tags}.
Plain shorthand is accepted only for random intercepts such as
\code{~ 1 | study} or nested intercepts such as \code{~ 1 | study/effect}; random
slopes require an explicit structure tag or the \code{||} diagonal shorthand.
The optional \code{R} argument supplies known covariance or correlation matrices
across random-effect grouping levels, following \code{metafor::rma.mv()} naming.
\code{R} is separate from the known sampling covariance \code{V}: \code{V} describes
sampling-error dependence between estimates, while \code{R} describes the
latent group-axis covariance of random intercepts. Current \code{R} support is
limited to random-intercept blocks. \code{Rscale} controls how each supplied
\code{R} matrix is scaled before fitting and accepts
\code{"cor"}, \code{"none"}, \code{"cor0"}, \code{"cov0"}, plus metafor-compatible aliases
\code{TRUE}/\code{1} for \code{"cor"}, \code{FALSE}/\code{0} for \code{"none"}, \code{2} for \code{"cor0"},
and \code{3} for \code{"cov0"}.
When \code{random} is used, shared sampled
random-effect blocks enter the conditional mean through BayesTools. With
\code{marginalize_estimate_level = TRUE}, a single one-to-one random-intercept
block is integrated into the likelihood variance; otherwise the likelihood
remains conditional on sampled random effects and uses only the known
sampling covariance \code{V}. With a single top-level random component, \code{scale}
models the row-wise total random-effect SD consumed by that component. With
multiple top-level random components, use a named list of \code{scale} formulas
whose names uniquely match the random components. Component names can be
ordinary text; RoBMA normalizes them internally for JAGS. Each formula
models the row-wise total SD for its component. A single \code{scale} formula is
rejected for multiple top-level random components because the target is
ambiguous.
When \code{scale} is a named list, \code{prior_scale} must either be omitted or use the
same named-list shape with one prior list per scale component, using the
same user-facing names. Post-fit scale predictions for random-formula
models keep these component scales separate rather than collapsing them into
one total \eqn{\tau}.
If \code{marginalize_estimate_level = TRUE}, a single random-intercept block that
maps one-to-one to estimates is compiled as a marginalized variance component
instead of sampled latent coefficients. This preserves estimate-level
heterogeneity semantics while keeping shared higher-level random effects
conditional. For known \code{R}, this automatic marginalization is available only
when BayesTools validates a one-column random-intercept block whose
\verb{Z K Z'} row-space contribution is diagonal and one-to-one with the fitted
rows; RoBMA then adds the BayesTools-prepared row multiplier to the known-\code{V}
extra variance. Other known-\code{R} blocks remain sampled or are rejected by
BayesTools validation. Ambiguous cases with multiple one-to-one blocks are
rejected.

The implementation intentionally omits the \code{weights} and \code{cluster} arguments
from \code{metafor::rma.mv()}. Use \code{random} for multilevel structures.
Estimate-unit \code{logLik()}, \code{add_loo()}, \code{add_waic()}, \code{residuals()}, and
\code{rstudent()} are available for known-\code{V} models. Bridge-sampling marginal
likelihoods via \code{add_marglik()} are available for fitted single-model
known-\code{V} objects. \code{rstandard()} is available for known-\code{V} models at
supported estimate and marginal conditioning depths.
}
\section{Diagnostic target semantics}{

The main \code{brma.mv()} post-fit methods intentionally use different targets:
\itemize{
\item \code{logLik(unit = "estimate")}, \code{add_loo()}/\code{loo()},
\code{add_waic()}/\code{waic()}, and \code{rstudent()} use estimate-unit log-score
targets. When known \code{V} induces dependence, these contributions are
Schur-complement conditionals \eqn{p(y_i \mid y_{-i}, \theta)} within
dependency blocks. They are existing-estimate diagnostics, not
independent new-estimate prediction targets.
When known random-effect covariance \code{R} is supplied, sampled random
effects remain conditioned in these estimate-depth targets. The known
\code{R} matrix shapes the posterior and prior for those random effects, but
it is not added again as a marginal \eqn{ZGZ'} covariance term. Supported
known-\code{R} blocks compiled as marginalized instead enter through the
diagonal extra variance term \eqn{\tau^2 r_i} prepared by BayesTools.
\item \code{dfbetas()} and \code{covratio()} use estimate-unit PSIS weights. With
correlated known \code{V}, their interpretation is parameter influence under
conditional estimate deletion, not independent-study deletion.
\item \code{add_marglik()}/\code{bridge_sampler()} uses the full joint fitted
likelihood corresponding to the selected known-\code{V} backend. It does not
use the estimate-wise conditional target from LOO/WAIC. Known \code{R}
enters this full joint target either through the latent random-effect
prior for sampled blocks or through the diagonal marginalized
known-\code{R} variance for supported marginalized blocks.
\item \code{hatvalues()}, marginal \code{rstandard()}, and \code{vif()} use a marginal
GLS covariance target based on \verb{V + ZGZ'}, where formula random effects
are marginalized through the BayesTools covariance metadata.
\item \code{dffits()} and \code{cooks.distance()} use fixed-location fitted-value
influence targets. Cluster/block LOO is deferred for \code{brma.mv()} because
its deletion target is a joint dependency block, not an independent row.
}
Default scalar heterogeneity summaries are not reported for \code{brma.mv()}
because known-\code{V} and random-formula structures need component-specific
summaries rather than a single \eqn{\tau}/\eqn{I^2} table.
}

\examples{
\dontrun{
V <- matrix(c(0.04, 0.01, 0.01, 0.09), 2, 2)
fit <- brma.mv(
  yi                        = c(0.10, 0.20),
  V                         = V,
  measure                   = "GEN",
  prior_unit_information_sd = 1,
  seed                      = 1,
  silent                    = TRUE
)
summary(fit)
}

}
\seealso{
\code{\link{random_effect_formula_tags}},
\code{\link{random_effect_prior_specification}}, \code{\link[=brma]{brma()}},
\code{\link[=summary.brma]{summary.brma()}}, \code{\link[=predict.brma]{predict.brma()}}
}
