% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BMA.glmm.R
\name{BMA.glmm}
\alias{BMA.glmm}
\title{Bayesian Model-Averaged Generalized Meta-Analysis}
\usage{
BMA.glmm(
  ai,
  bi,
  ci,
  di,
  n1i,
  n2i,
  x1i,
  x2i,
  t1i,
  t2i,
  weights,
  mods,
  scale,
  study_ids,
  data,
  slab,
  subset,
  measure = "OR",
  prior_effect,
  prior_heterogeneity,
  prior_mods,
  prior_scale,
  prior_heterogeneity_allocation,
  prior_baserate,
  prior_lograte,
  prior_effect_null,
  prior_heterogeneity_null,
  prior_mods_null,
  prior_scale_null,
  prior_heterogeneity_allocation_null,
  standardize_continuous_predictors = TRUE,
  set_contrast_factor_predictors = "treatment",
  prior_unit_information_sd,
  rescale_priors = 1,
  prior_informed_field,
  prior_informed_subfield,
  sample = 5000,
  burnin = 2000,
  adapt = 500,
  chains = 3,
  thin = 1,
  parallel = FALSE,
  autofit = FALSE,
  autofit_control = set_autofit_control(),
  convergence_checks = set_convergence_checks(),
  seed = NULL,
  silent = TRUE,
  ...
)
}
\arguments{
\item{ai}{a vector of the number of events in the treatment/experimental group.}

\item{bi}{a vector of the number of non-events in the treatment/experimental group.}

\item{ci}{a vector of the number of events in the control group.}

\item{di}{a vector of the number of non-events in the control group.}

\item{n1i}{a vector of the sample size in the treatment/experimental group.}

\item{n2i}{a vector of the sample size in the control group.}

\item{x1i}{a vector of the number of events in the treatment/experimental group
(for Poisson data).}

\item{x2i}{a vector of the number of events in the control group (for Poisson data).}

\item{t1i}{a vector of the person-time in the treatment/experimental group.}

\item{t2i}{a vector of the person-time in the control group.}

\item{weights}{an optional vector of weights.}

\item{mods}{an optional vector, matrix, data.frame, or formula specifying
moderators (or meta-regressors).}

\item{scale}{an optional vector, matrix, data.frame, or formula specifying
scale predictors (for location-scale models).}

\item{study_ids}{an optional vector of study identifiers (used for clustering).}

\item{data}{an optional data frame containing the variables.}

\item{slab}{an optional vector of study labels.}

\item{subset}{an optional logical or numeric vector specifying a subset of
data to be used.}

\item{measure}{a character string specifying the effect size measure.
Defaults to \code{"GEN"}. Other options are \code{"SMD"}, \code{"OR"}, \code{"RR"}, \code{"RD"},
\code{"HR"}, \code{"IRR"}, \code{"ZCOR"}.}

\item{sample}{numeric. Number of MCMC samples to save. Defaults to \code{5000}.}

\item{burnin}{numeric. Number of burn-in iterations. Defaults to \code{2000}.}

\item{adapt}{numeric. Number of adaptation iterations. Defaults to \code{500}.}

\item{chains}{numeric. Number of MCMC chains. Defaults to \code{3}.}

\item{thin}{numeric. Thinning interval. Defaults to \code{1}.}

\item{parallel}{logical. Whether to run MCMC chains in parallel. Defaults to \code{FALSE}.}

\item{autofit}{logical. Whether to automatically extend the MCMC chains if convergence is not met.
Defaults to \code{FALSE}.}

\item{autofit_control}{list of autofit control settings. See \code{\link[=set_autofit_control]{set_autofit_control()}} for details.}

\item{convergence_checks}{list of convergence check settings. See \code{\link[=set_convergence_checks]{set_convergence_checks()}} for details.}

\item{seed}{numeric. Random seed for reproducibility. Defaults to \code{NULL}.}

\item{silent}{logical. Whether to suppress output. Defaults to \code{RoBMA.get_option("silent")}.}

\item{...}{additional arguments.}
}
\value{
\code{BMA.glmm} returns an object of class \code{"BMA.glmm"}.
}
\description{
Function for fitting Bayesian model-averaged meta-analytic models
directly to binary or count data using a generalized linear mixed model (GLMM)
framework. Unlike \code{\link{RoBMA}}, this function does not adjust for
publication bias, as weight function and regression-based bias adjustment
methods are not available for GLMM outcomes.
}
\details{
\code{BMA.glmm} combines the data input style of \code{\link{brma.glmm}} with
the mixture prior specification of \code{\link{RoBMA}} for Bayesian model-averaging.

Model for odds ratios (\code{measure = "OR"}) uses a binomial-normal model
as described in \insertCite{jackson2018comparison;textual}{RoBMA}.

Model for incidence rate ratios (\code{measure = "IRR"}) uses a Poisson-normal
model as described in \insertCite{bagos2009mixed;textual}{RoBMA}.
}
\seealso{
\code{\link[=brma.glmm]{brma.glmm()}} \code{\link[=RoBMA]{RoBMA()}} \code{\link[=summary.brma]{summary.brma()}} \code{\link[=plot.brma]{plot.brma()}}
}
