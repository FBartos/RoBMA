% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iwmde-api.R
\name{plot_iwmde_marginal_means_diagnostics}
\alias{plot_iwmde_marginal_means_diagnostics}
\title{Plot IWMDE diagnostics for estimated marginal means}
\usage{
plot_iwmde_marginal_means_diagnostics(
  object,
  parameter = NULL,
  type = NULL,
  levels = NULL,
  marginal_means_object = NULL,
  n_marginal_samples = 10000,
  n_points = 150,
  max_samples = 400,
  normalization_points = NULL,
  normalization_prob = 0.999,
  method = c("q_grid_cmde", "iwmde"),
  display_grid = c("adaptive", "uniform"),
  plot = TRUE,
  as_data = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{a fitted \code{brma}, \code{BMA}, or \code{RoBMA} object.}

\item{parameter}{optional moderator term(s) to plot. Defaults to all
available marginal-mean terms.}

\item{type}{marginal-mean type. Only \code{"averaged"} is currently
supported for IWMDE diagnostics.}

\item{levels}{optional marginal-mean level names to keep.}

\item{marginal_means_object}{optional precomputed \code{marginal_means.brma}
object. If \code{NULL}, it is computed from \code{object}.}

\item{n_marginal_samples}{number of samples/grid points passed to
\code{marginal_means()} when \code{marginal_means_object = NULL}.}

\item{n_points}{number of grid points per IWMDE/KDE curve.}

\item{max_samples}{maximum posterior rows used by IWMDE per parameter.}

\item{normalization_points}{number of internal-scale grid points used for
row-wise conditional normalization. Defaults to \code{max(50, n_points)}.}

\item{normalization_prob}{posterior central probability used to construct
the normalization grid before forcing it to include the displayed range.}

\item{method}{density estimator. \code{"q_grid_cmde"} uses row-normalized
q-grid conditional densities. \code{"iwmde"} uses Chen-style moment-matched
weights: conditional normal weights for unconstrained targets and
power/exponential weights for bounded or one-sided targets.}

\item{display_grid}{support-point placement for the plotted IWMDE curve.
\code{"adaptive"} combines a uniform backbone, posterior quantiles, and
pilot-density height/curvature points. \code{"uniform"} uses equally spaced
points on the displayed parameter scale.}

\item{plot}{whether to draw the square diagnostic plot matrix.}

\item{as_data}{whether to return only the computed plot data.}

\item{seed}{optional random seed used when thinning posterior rows.}

\item{...}{additional graphical arguments passed to the base plotting path.}
}
\value{
A list with one entry per marginal-mean level. Returned visibly when
\code{as_data = TRUE} or \code{plot = FALSE}, and invisibly after plotting.
}
\description{
Compare sample KDE, histogram, and IWMDE curves for estimated
marginal means. Each marginal mean is treated as the linear function of the
fitted primitive formula coefficients stored by \code{marginal_means()}.
Diagnostics are computed on the fitted linear predictor scale.
}
\section{Stability}{

This is a developer-facing diagnostic API for validating the IWMDE feature.
The plotting contract and tuning arguments may be simplified after review.
}

