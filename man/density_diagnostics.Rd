% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density-diagnostics.R
\name{density_diagnostics}
\alias{density_diagnostics}
\title{Extract qCMDE/IWMDE Density Diagnostics}
\usage{
density_diagnostics(object, ...)
}
\arguments{
\item{object}{an object returned by \code{\link[=hypothesis]{hypothesis()}} with
\code{density_method = "qCMDE"} or \code{density_method = "IWMDE"}, or an
\code{RoBMA_density_ordinate_error} caught from a rejected point-ordinate
computation.}

\item{...}{unused.}
}
\value{
A data frame of class \code{RoBMA_density_diagnostics}, with one row per
computed point ordinate. Columns identify the estimator, parameter, requested
and evaluated values, schema/source provenance, row counts, active mass,
relative MCSE, ESS, largest contribution share, finite terms, normalization
and quadrature checks, fixed-sampling state, policy thresholds, weight
fallbacks, status, and warnings.

The exact columns, in order, are \code{schema_version}, \code{algorithm_version},
\code{source_fingerprint}, \code{estimator}, \code{density_method}, \code{parameter}, \code{level},
\code{requested_value}, \code{evaluation_value}, \code{requested_samples},
\code{achieved_row_budget}, \code{eligible_rows},
\code{evaluated_rows}, \code{finite_terms}, \code{active_mass}, \code{relative_mcse},
\code{sampling_relative_mcse},
\code{sampling_fraction}, \code{mcmc_uncertainty_scope},
\code{sampling_uncertainty_type}, \code{ess}, \code{max_weight_share},
\code{normalization_relative_error}, \code{stability_metric},
\code{stability_relative_error}, \code{ordinate_relative_change},
\code{quadrature_relative_change}, \code{target_relative_mcse},
\code{stability_warning_threshold}, \code{stability_rejection_threshold},
\code{quadrature_warning_threshold}, \code{quadrature_rejection_threshold},
\code{warning_relative_mcse}, \code{warning_min_finite_terms}, \code{warning_min_ess},
\code{warning_max_weight_share}, \code{all_rows_used}, \code{target_met},
\code{precision_target_met},
\code{sampling_target_met}, \code{bf_grade_met}, \code{n_weight_fallbacks},
\code{weight_fallback_reasons}, \code{status}, and \code{warnings}.
}
\description{
Extracts the compact reliability diagnostics attached to a
qCMDE/IWMDE hypothesis result. The diagnostics describe the posterior
ordinate computation. They do not include uncertainty in the prior ordinate
or, for IWMDE, uncertainty from estimating its conditional weight function.
}
\details{
Point ordinates evaluate one fixed posterior-row sample whose size is chosen
by \code{samples} before any ordinate contributions are computed. Precision,
effective-sample-size, and contribution-concentration diagnostics never
determine whether that finite estimate is returned. An unmet
\code{target_relative_mcse} produces a warning; increase \code{samples} or set it to
\code{Inf} to use every eligible row. The
table records the requested and achieved sample sizes, \code{all_rows_used},
whether the precision target was met, and whether the ordinate passed every
independent numerical-stability gate. Finite row budgets use a reproducible simple random sample
without replacement. For a purely continuous ordinate, \code{relative_mcse} and \code{ess}
describe the selected continuous posterior-row sequence. For a mixed
point/continuous ordinate evaluated on every active continuous row, they use
the full conditioned-chain sequence, with zero contributions for inactive
rows, and therefore include uncertainty in the active/product-space indicator
mass. When only a subset of active rows is evaluated, these MCMC diagnostics
are unavailable. \code{sampling_relative_mcse} separately estimates
finite-population row-sampling uncertainty with its sampling fraction. Every
available precision check is reported without selecting estimates by its value.

The reliability policy warns when relative MCSE is at least 5 percent, ESS is
below 100, the largest contribution share is at least 20 percent, or fewer
than 100 finite contributions remain. These same-sample diagnostics do not
reject a finite fixed-design estimate. Their warning thresholds are returned
as columns rather than being implicit.

qCMDE's full method diagnostics distinguish \code{pilot_normalization_integral}
from \code{final_normalization_integral}; IWMDE uses
\code{support_grid_normalization_integral}. This compact accessor reports their
common normalization check as \code{normalization_relative_error}. The active
policy check is identified by \code{stability_metric}: qCMDE checks posterior
ordinate movement, whereas IWMDE checks normalization mass. The table also
reports the stability and adaptive-quadrature warning/rejection thresholds.
}
\examples{
\dontrun{
result <- hypothesis(
  fit,
  hypothesis     = "mu != 0 vs mu = 0",
  density_method = "qCMDE"
)
density_diagnostics(result)

failed <- tryCatch(
  hypothesis(
    fit,
    hypothesis     = "mu != 0 vs mu = 0",
    density_method = "IWMDE"
  ),
  RoBMA_density_ordinate_error = identity
)
density_diagnostics(failed)
}

}
\seealso{
\code{\link[=hypothesis]{hypothesis()}}
}
