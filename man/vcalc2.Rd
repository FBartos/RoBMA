% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcalc2.R
\name{vcalc2}
\alias{vcalc2}
\title{Sampling covariance with retained construction metadata}
\usage{
vcalc2(
  vi,
  cluster,
  subgroup,
  obs,
  type,
  time1,
  time2,
  grp1,
  grp2,
  w1,
  w2,
  data,
  rho,
  phi,
  rvars,
  checkpd = TRUE,
  nearpd = FALSE,
  sparse = FALSE,
  ...
)
}
\arguments{
\item{vi, cluster, subgroup, obs, type, time1, time2, grp1, grp2, w1, w2, data, rho, phi, rvars}{Arguments passed unchanged to \code{\link[metafor:vcalc]{metafor::vcalc()}}.}

\item{checkpd, nearpd, sparse}{Logical arguments passed unchanged to
\code{\link[metafor:vcalc]{metafor::vcalc()}}.}

\item{...}{Additional arguments passed unchanged to \code{\link[metafor:vcalc]{metafor::vcalc()}}.}
}
\value{
The object returned by \code{\link[metafor:vcalc]{metafor::vcalc()}} with retained RoBMA
construction metadata.
}
\description{
\code{vcalc2()} is a metadata-preserving wrapper around \code{\link[metafor:vcalc]{metafor::vcalc()}}. It
returns the same sampling covariance while retaining enough construction
information for RoBMA to use a certified diagonal-plus-factor
representation when one is available. Calls outside the supported factor
contract remain ordinary dense sampling covariances.

The optimized representation is currently available when \code{type}, \code{obs}, and
a numeric two-element \code{rho} are supplied without \code{grp1}, \code{grp2}, \code{time1},
\code{time2}, \code{w1}, \code{w2}, or \code{rvars}, and when \code{nearpd = FALSE}. This covers the
common within- and between-construct correlation specification. The
covariance returned by \code{metafor::vcalc()} remains authoritative.
}
\examples{
\dontrun{
if (requireNamespace("metafor", quietly = TRUE)) {
  dat <- data.frame(
    vi      = c(0.04, 0.05, 0.03),
    study   = c(1, 1, 1),
    outcome = c("a", "a", "b"),
    effect  = 1:3
  )
  V <- vcalc2(
    vi, cluster = study, type = outcome, obs = effect,
    rho = c(0.6, 0.3), data = dat
  )
}
}

}
