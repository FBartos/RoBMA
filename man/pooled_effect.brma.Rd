% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brma.wrappers.R
\name{pooled_effect.brma}
\alias{pooled_effect.brma}
\title{Pooled Effect Size for brma Objects}
\usage{
\method{pooled_effect}{brma}(
  object,
  bias_adjusted = TRUE,
  probs = c(0.025, 0.975),
  as_samples = FALSE,
  ...
)
}
\arguments{
\item{object}{a fitted brma object}

\item{bias_adjusted}{whether to adjust for publication bias. Defaults to
\code{TRUE}, which returns bias-corrected estimates. Set to \code{FALSE}
to obtain estimates that include publication bias effects.}

\item{probs}{quantiles of the posterior distribution to be displayed.
Defaults to \code{c(.025, .975)} for 95\% credible intervals.}

\item{as_samples}{whether posterior samples should be returned instead of
a summary table. Defaults to \code{FALSE}.}

\item{...}{additional arguments (currently ignored)}
}
\value{
If \code{as_samples = FALSE}, returns a \code{brma.predict} object
containing a summary table with the pooled effect estimate and credible
interval. If \code{as_samples = TRUE}, returns a matrix of posterior samples.
}
\description{
Computes the pooled (aggregated) effect size estimate
from a fitted brma object by averaging across the moderator model matrix.
}
\details{
This function is a convenience wrapper around \code{predict.brma(...,
type = "terms", newdata = TRUE)}.

For meta-regression models, the pooled effect averages the effect size
estimate across moderator levels proportionately to the levels observed
in the data. This provides an estimate representative of the sample of
studies.

For models without moderators, this returns the single mu parameter.
}
\examples{
\dontrun{
# fit a brma model
fit <- brma(yi ~ 1, sei = sei, data = dat)

# get pooled effect
pooled_effect(fit)

# get posterior samples
samples <- pooled_effect(fit, as_samples = TRUE)
}

}
\seealso{
\code{\link[=predict.brma]{predict.brma()}}, \code{\link[=pooled_heterogeneity]{pooled_heterogeneity()}}, \code{\link[=blup]{blup()}}
}
