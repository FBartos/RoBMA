% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{update.brma}
\alias{update.brma}
\title{Update a brma Fit}
\usage{
\method{update}{brma}(
  object,
  formula. = NULL,
  ...,
  sample_extend = NULL,
  slab = NULL,
  autofit_control = NULL,
  convergence_checks = NULL,
  recompute = c("all", "drop"),
  parallel = NULL,
  cores = NULL,
  silent = NULL,
  seed = NULL,
  evaluate = TRUE
)
}
\arguments{
\item{object}{a fitted \code{brma} object.}

\item{formula.}{unsupported; included for compatibility with
\code{\link[stats]{update}}.}

\item{...}{unsupported additional arguments.}

\item{sample_extend}{integer. Number of additional samples per chain.}

\item{slab}{optional character vector of study labels. Updating labels does
not refit or extend the model.}

\item{autofit_control}{list of autofit control settings. Values are merged
with the existing settings before extending.}

\item{convergence_checks}{list of convergence check settings. Values are
merged with the existing settings and used to re-check the fit.}

\item{recompute}{whether cached \code{loo}, \code{waic}, and
\code{marglik} values already stored in \code{object} should be recomputed
after extension (\code{"all"}) or dropped with a warning (\code{"drop"}).}

\item{parallel}{logical. Whether to extend chains in parallel.}

\item{cores}{integer. Number of cores to use when \code{parallel = TRUE}.}

\item{silent}{logical. Whether to suppress JAGS output during extension.}

\item{seed}{optional seed used before extending.}

\item{evaluate}{unsupported; included for compatibility with
\code{\link[stats]{update}}.}
}
\value{
The updated \code{brma} object.
}
\description{
Extends an existing fitted \code{brma} object by additional MCMC samples,
updates study labels, and optionally recomputes cached fit-dependent
quantities.
}
\details{
Extending a fit adds posterior samples only. It does not rerun adaptation or
burn-in. Prior, data, and model-structure updates are intentionally not
supported by this method. For \code{brma.mv()} objects, the stored \code{V},
\code{R}, known-\code{V} backend, and marginalized random-effect metadata are
preserved because the fitted model structure is reused.

Named \code{NULL} elements in \code{autofit_control} and
\code{convergence_checks} explicitly disable or clear the corresponding
nullable setting. Omitted elements, a top-level \code{NULL}, and an empty
\code{list()} inherit the setting stored in \code{object}.
}
\examples{
\dontrun{
fit <- update(fit, sample_extend = 1000)
fit <- update(fit, slab = paste("Study", seq_len(nobs(fit))))
}

}
