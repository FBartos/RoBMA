% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{print_prior}
\alias{print_prior}
\alias{print_prior.prior}
\alias{print_prior.brma}
\title{Print Prior Distributions}
\usage{
print_prior(x, ...)

\method{print_prior}{prior}(x, ...)

\method{print_prior}{brma}(x, parameter = "mu", parameter_mods, parameter_scale, ...)
}
\arguments{
\item{x}{a \code{brma}, \code{BMA}, or \code{RoBMA} object, or a prior
distribution object.}

\item{...}{additional arguments passed to the prior printing method.}

\item{parameter}{character. Base parameter to print. Defaults to \code{"mu"}.
Common options are \code{"mu"}, \code{"tau"}, \code{"rho"}, \code{"PET"},
\code{"PEESE"}, \code{"omega"}, and \code{"bias"}. Moderator and scale
terms can also be selected by name when unambiguous.}

\item{parameter_mods}{character. Moderator term to print.
Use \code{"intercept"} for the adjusted effect in meta-regression models.}

\item{parameter_scale}{character. Scale-regression term to print.
Use \code{"intercept"} for the heterogeneity intercept in location-scale models.}
}
\value{
\code{print_prior} invisibly returns the selected prior distribution.
If multiple parameters are requested, a named list of prior distributions is
returned invisibly.
}
\description{
\code{print_prior} prints prior distributions stored in
\code{brma}, \code{BMA}, and \code{RoBMA} objects.
This is a user-facing helper for inspecting priors without extracting them
from the internal \code{$priors} list.
}
\examples{
\dontrun{
priors <- BMA(yi = yi, sei = sei, data = dat, measure = "SMD", only_priors = TRUE)
print_prior(priors, parameter = "mu")
print_prior(priors, parameter = "tau")
print_prior(priors, parameter_mods = "x")
}

}
\seealso{
\code{\link[=plot_prior]{plot_prior()}} \code{\link[=BMA]{BMA()}} \code{\link[=RoBMA]{RoBMA()}} \code{\link[=brma]{brma()}} \code{\link[=prior]{prior()}}
}
