% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covratio.R
\name{covratio.brma}
\alias{covratio.brma}
\title{COVRATIO for brma Objects}
\usage{
\method{covratio}{brma}(model, type = "mods", ...)
}
\arguments{
\item{model}{a fitted brma object.}

\item{type}{type of parameters to be summarized. Defaults to \code{"mods"}
(for the effect size and meta-regression coefficients).}

\item{...}{additional arguments (currently ignored).}
}
\value{
A numeric vector of COVRATIO values, one for each observation.
}
\description{
Computes COVRATIO for a fitted brma object. COVRATIO measures
the change in the determinant of the covariance matrix of the estimates
when observation \eqn{i} is removed.
}
\details{
COVRATIO is computed using importance sampling weights to approximate the
leave-one-out covariance matrices without refitting the model.

\deqn{COVRATIO_i = \frac{\det(Cov(\beta)_{-i})}{\det(Cov(\beta))}}

Values > 1 indicate that the observation improves precision (decreases
variance), while values < 1 indicate that the observation decreases precision
(increases variance).
}
\examples{
\dontrun{
# fit a brma model
fit <- brma(yi ~ 1, sei = sei, data = dat)
fit <- add_loo(fit)

# compute COVRATIO
covratio(fit)
}

}
\seealso{
\code{\link{influence.brma}}, \code{\link{dffits.brma}}, \code{\link{cooks.distance.brma}}
}
